name: Daily leads report

on:
  schedule:
    - cron: '0 17 * * *'
  workflow_dispatch: {}

jobs:
  trigger-report:
    runs-on: ubuntu-latest
    steps:
      - name: Call daily report endpoint
        run: |
          curl -sf "https://zheludkin-apd-stroy-bot-ce16.twc1.net/cron/daily-report?secret=${{ secrets.CRON_SECRET }}"
